package managerTest;

import manager.*;
import model.Status;
import model.Task;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.Collection;

public class ManagerTest {
/*    @Test
    void defaultHistoryManager() {
        HistoryManager historyManager = Managers.getDefaultHistory();

        historyManager.add(new Task("Task", "Description", Status.NEW, 1));
        Collection<Task> history = historyManager.getHistory();
        Assertions.assertEquals(1, history.size());
    }
    @Test
    void defaultTaskManager() {
        TaskManager taskManager = Managers.getDefaultTaskManager();

        Task task = new Task("Task", "Description", Status.NEW, 1);
        taskManager.addTask(task);
        Task task1 = taskManager.getTaskById(1);
        Assertions.assertEquals(task, task1);
    }*/
}
